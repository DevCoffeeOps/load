generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./running_shoe_stores.db"
}

model RunningShoeStore {
  id    Int @id @default(autoincrement())
  name  String
  streetAddress String
  lng   Float
  lat   Float
}
